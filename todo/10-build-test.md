# Step 10: 编译测试

## 目标

确保修改后的代码能编译通过，测试通过。

## 命令

```bash
cargo build 2>&1
```

修复所有编译错误，然后：

```bash
cargo test 2>&1
```

## 预期问题

- 类型不匹配（删除的字段/结构体被引用）
- 未使用的导入/变量
- 测试代码中引用了被删除的类型

## 修复策略

- 逐个修复编译错误
- 测试代码中涉及 auth 流程的测试需要更新或删除
- 保留核心功能测试（速率限制、重试、心跳等）